myApp.controller("RegistrationController", ["$scope", "$http", "$location", "AuthFactory", "UserFactory", function($scope, $http, $location, AuthFactory, UserFactory) {
    console.log("RegistrationController works");

    // THIS IS WHERE WE WANT TO REDIRECT USERS WHO ARE LOGGING IN FOR THE FIRST TIME
    // also it is where users can register for a sprint
    // also grapedog


}]);
